import 'package:flutter/material.dart';

class homescreen extends StatefulWidget {//to manage widget
  const homescreen({Key? key}) : super(key: key);

  @override
  State<homescreen> createState() => _homescreenState();//creating state for widget called homescreen
}

class _homescreenState extends State<homescreen> {//to manage state
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
